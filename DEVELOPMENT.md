# Development

This is a [Bashly](https://bashly.dev) project. Regenerate the CLI with:

```bash
bashly generate
```

The repository includes Bats tests for the library layers and command-level
behavior.

See [op.conf](op.conf) for additional development commands.
